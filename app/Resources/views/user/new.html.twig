{% extends 'base.html.twig' %}

{% block body %}
    <h1>User creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            {% if app.user.roles[0] == constant('AppBundle\\Entity\\User::ROLE_ADMIN')%}
                <a href="{{ path('user_index') }}">Back to the list</a>
            {% else %}
                <a href="{{ path('homepage') }}">FlyAround</a>
            {% endif %}
        </li>
    </ul>
{% endblock %}
