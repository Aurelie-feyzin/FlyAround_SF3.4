{%  extends 'base.html.twig' %}

{%  block body %}
    <h1>Création d'un nouveau commentaire</h1>

    {{  form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Creer" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('review_index') }}">Retour à la liste</a>
        </li>
    </ul>
{% endblock %}